# 💬 RAG Chatbot Frontend

This is the **frontend interface** for the AI Chatbot with YouTube integration.  
It provides a lightweight, interactive chat UI built using **React + TypeScript + Vite**, allowing users to have intelligent conversations with the AI agent hosted on the backend.

The frontend communicates with the backend (`/generate` endpoint) to:
- Send user messages  
- Receive responses generated by the AI Agent  
- Display a seamless chat experience with real-time updates

---

## 🚀 Features

- ⚡ Built with **React + TypeScript + Vite**
- 🧠 Connects directly to your **LangGraph + Claude backend**
- 💬 Real-time chat experience with smooth scrolling
- ⏳ Typing indicator with animated dots
- 🔁 “New Chat” reset button to start fresh conversations
- 📱 Responsive and minimal design using pure CSS


---

## ⚙️ Environment Variables

Create a `.env` file in the root directory:

```bash
# URL of the backend API (default: http://localhost:3000)
VITE_API_URL=http://localhost:3000


---

## 🧰 Installation & Setup

# 1️⃣ Clone the repository
git clone https://github.com/<your-username>/AIChatBotWithYoutube.git
cd AIChatBotWithYoutube/frontend

# 2️⃣ Install dependencies
npm install

# 3️⃣ Run the development server
npm run dev

# By default, the app will run on:
http://localhost:5173
